name: Build and Publish Releases

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build for ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Windows x64
          - os: windows-latest
            platform: windows-x64
            target: node18-win-x64
            output_name: gura-bot-win-x64.exe

          # Linux x64
          - os: ubuntu-latest
            platform: linux-x64
            target: node18-linux-x64
            output_name: gura-bot-linux-x64

          # macOS arm64 (Apple Silicon)
          - os: macos-latest
            platform: macos-arm64
            target: node18-macos-arm64
            output_name: gura-bot-mac-arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        env:
          YOUTUBE_DL_SKIP_DOWNLOAD: "true"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm install

      - name: Install pkg
        run: npm install -g pkg

      - name: Build binary
        run: pkg package.json --targets ${{ matrix.target }} --output ./dist/${{ matrix.output_name }}

      - name: Upload artifact to build job
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.platform }}
          path: ./dist/

  release:
    name: Create GitHub Release & Notify Discord
    needs: build
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Download all artifacts (if built successfully)
        if: needs.build.result == 'success'
        uses: actions/download-artifact@v4
        with:
          path: ./dist
          merge-multiple: true

      - name: Sync Release Notes to GitHub Release
        if: needs.build.result == 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node scripts/sync-releases.js ${{ github.ref_name }}

      - name: Send Discord Webhook Notification (Success)
        if: needs.build.result == 'success'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          node scripts/notify-discord.js ${{ github.ref_name }} success

      - name: Send Discord Webhook Notification (Failure)
        if: needs.build.result != 'success'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          node scripts/notify-discord.js ${{ github.ref_name }} failure

      - name: Upload Binaries to Release
        if: needs.build.result == 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload ${{ github.ref_name }} ./dist/* --clobber
